{
  "name": "mybatchjobs",
  "version": "1.0.0",
  "description": "cron based batch Jobs in Node.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.8.2"
  }
}
